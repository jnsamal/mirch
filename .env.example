# URL of the deployed Mirch backend (e.g. https://mirch-api.example.com).
# Leave unset for local dev — Vite proxies /api to http://localhost:4000
# (see vite.config.js), so no CORS setup is needed during development.
VITE_API_URL=
